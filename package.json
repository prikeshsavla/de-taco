{
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "start": "node .output/server/index.mjs",
    "bot:dev": "node -r dotenv/config slack/app.js dotenv_config_path=.env",
    "bot:start": "node slack/app.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.6.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "nuxt3": "latest"
  }
}
